You are an expert Linux system administrator helping fix a slow SMTP issue on a Google Cloud VM.

### Current Setup:
- Website: americanseekersacademy.com is hosted on a Google Cloud Compute Engine VM with public IP 34.111.179.208.
- All outbound transactional / auto emails are sent exclusively via the **Brevo API** (not through local SMTP). The Brevo API calls must continue working exactly as they are now.
- Email receiving / mailboxes for @americanseekersacademy.com are handled by Hostinger (MX records point there).
- Brevo support and SuperTool are showing: SMTP Connect to 34.111.179.208 times out after 15 seconds. This is causing domain health checks to fail and blocking login/verification.

### Goal:
Disable or stop the unnecessary local mail server (likely Postfix, Exim, or similar) on this VM so that nothing listens on SMTP ports (25, 587, 465). This should make external tools like Brevo see the IP as having no slow mail service, without breaking the website or the Brevo API sending.

### Instructions - Do this safely step by step:

1. First, check what mail services are currently running:
   - `sudo systemctl status postfix`
   - `sudo systemctl status exim`
   - `sudo netstat -tuln | grep -E ':25|:465|:587'`
   - `sudo ss -tuln | grep -E ':25|:465|:587'`
   - Also check: `ps aux | grep -E 'postfix|exim|sendmail'`

2. If Postfix (or another mail server) is installed and running:
   - Stop the service: `sudo systemctl stop postfix` (or the relevant service)
   - Disable it from starting on boot: `sudo systemctl disable postfix`
   - Mask it for extra safety (optional but recommended): `sudo systemctl mask postfix`

3. Close the SMTP ports in the firewall (ufw or iptables/firewalld):
   - If using ufw: `sudo ufw deny 25`, `sudo ufw deny 465`, `sudo ufw deny 587`
   - Then `sudo ufw reload`
   - If using firewalld: deny the ports and reload.

4. Verify after changes:
   - Run the netstat/ss command again — no processes should be listening on 25/465/587.
   - Test from outside: someone should no longer get a 15-second timeout when trying to connect to port 25 on 34.111.179.208.

5. Important: Do NOT remove or break anything related to the web server (nginx, Apache, Node.js, etc.), PHP, databases, or any Brevo API code/integration. The website must stay fully operational.

6. After making changes, provide a clear summary of exactly what you did, the commands used, and the final verification output.

If no mail server is running but ports are still open or slow, suggest checking for other processes or Google Cloud firewall rules.

Be very careful — only stop/disable mail-related services. Confirm with me before making permanent changes if anything looks risky.